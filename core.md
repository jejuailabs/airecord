# core.md — 실시간 통역 SaaS 프로젝트 헌법

> **이 파일은 프로젝트의 단일 진입점이다.**
> Claude Code는 어떤 작업을 시작하든 이 파일을 먼저 읽고, 해당 작업에 맞는 `docs/` 문서를 이어서 읽는다.
> 이 문서와 `docs/` 하위 문서가 충돌하면 **`docs/` 하위 문서가 우선**한다(더 구체적이므로). 단, 아래 **§3 불변 원칙**은 어떤 문서보다 우선한다.

- **제품 코드네임:** `sotong` (임시. 정식 브랜드 확정 시 `docs/05-design-system.md`와 함께 일괄 교체)
- **문서 기준일:** 2026-07-26
- **1줄 정의:** 줌 주소 하나를 넣거나 마이크 버튼 하나를 누르면, 나머지는 전부 자동으로 처리되는 실시간 통역 SaaS.

---

## 1. 무엇을 만드는가

번역 엔진은 **하나**를 공유하고, 상황에 따라 **진입점(입출력 어댑터)만 두 갈래**로 나눈다.

| | **모드 A — 화상회의** | **모드 B — 대면** |
|---|---|---|
| 유저 액션 | 회의 주소 붙여넣기 | 마이크 버튼 클릭 |
| 최우선 가치 | 심플함 · 설치 zero | 지연 최소화 |
| 오디오 입력 | 회의 봇(Recall.ai)이 회의에 입장해 캡처 | 브라우저 마이크(지향성 마이크 권장) |
| 결과 전달 | 회의 채팅에 자막 링크 자동 게시 → 웹 자막 뷰어 | 모니터 자막(주) + 헤드셋 음성(보조) |
| 실행 위치 | Realtime Worker (서버 릴레이) | 브라우저 ↔ 모델 직결 (WebRTC) |

두 모드는 **같은 번역 코어**를 호출한다. 다른 것은 오디오가 들어오고 나가는 통로뿐이다.

---

## 2. 문서 지도

| 파일 | 무엇이 들어있나 | 언제 읽나 |
|---|---|---|
| `core.md` (이 파일) | 불변 원칙, 문서 지도, 용어, 완료 정의 | **항상** |
| `docs/01-architecture.md` | 시스템 구성도, 런타임 경계, 데이터 흐름, 왜 Vercel만으로 안 되는가 | 구조 관련 모든 작업 |
| `docs/02-stack-and-setup.md` | 기술 스택 확정 목록, 레포 구조, 환경변수, 로컬 실행 | 프로젝트 초기 세팅 |
| `docs/03-data-model-and-auth.md` | Firebase Google 로그인, 워크스페이스 모델, Firestore 스키마, 보안 규칙 | 데이터/인증 작업 |
| `docs/04-translation-pipeline.md` | 번역 코어 추상화, 모드 A/B 구현, 지연 예산, 폴백 | 번역 기능 구현 |
| `docs/05-design-system.md` | 색·타이포·간격 토큰, 라이트/다크, 자막 가독성 규격 | UI 작업 전부 |
| `docs/06-screens-and-i18n.md` | 라우트 트리, 화면별 스펙, next-intl 다국어 | 화면 구현 |
| `docs/07-pricing-and-cost-model.md` | API 원가 실측 단가, 분당 원가 계산식, 요금제 설계, 미터링 | 과금·사용량 작업 |
| `docs/08-compliance-and-roadmap.md` | 녹음 고지·동의, 개인정보 보관, 단계별 로드맵, 완료 기준 | 릴리스 계획·법무 관련 |

---

## 3. 불변 원칙 (이걸 어기는 구현은 반려)

1. **설정이 아니라 사용만 남긴다.**
   유저가 만지는 값은 **언어 선택뿐**. 모델 선택, 청크 크기, 샘플레이트, 품질 슬라이더 같은 것을 UI에 노출하지 않는다. 필요하면 관리자 설정에 숨긴다.

2. **자막이 1순위, 음성이 2순위다.**
   음성 번역은 연속 발화에서 드리프트가 누적되는 **구조적 한계**를 가진다(엔지니어링으로 제거 불가). 따라서 자막을 기본 채널로 두고 음성은 옵션으로 둔다. "음성이 늦다"는 버그 리포트는 버그가 아니라 설계 전제다 — 대신 **자막은 절대 늦으면 안 된다**.

3. **번역 코어는 하나다.**
   모드 A와 모드 B가 서로 다른 번역 구현을 갖지 않는다. `TranslationEngine` 인터페이스 하나를 두고, 어댑터만 갈아 끼운다. (`docs/04`)

4. **기성 인프라를 산다.**
   회의 봇 조인 로직, 번역 모델, 인증, 결제를 직접 만들지 않는다. MVP에서 자체 봇 개발은 **금지**.

5. **로그는 기능이 아니라 파이프라인의 부산물이다.**
   이미 흐르는 데이터를 버리지 않고 저장하는 것뿐. 별도 녹음 모듈을 만들지 않는다.

6. **분(minute)은 곧 돈이다.**
   번역 세션이 열려 있는 모든 시간은 실비가 나간다. 세션을 여는 코드는 반드시 **하드 캡·하트비트·타임아웃**을 함께 구현한다. 이것 없이 세션을 여는 PR은 머지하지 않는다. (`docs/07 §5`)

7. **추정치는 추정치라고 표시한다.**
   문서에 적힌 API 단가·성능 수치는 2026-07-26 기준 조사값이다. 코드에 하드코딩하지 말고 설정값으로 빼고, 값 옆에 출처 주석을 단다.

---

## 4. 용어 정의 (코드 전체에서 이 단어만 쓴다)

| 용어 | 코드 식별자 | 의미 |
|---|---|---|
| 워크스페이스 | `workspace` | 과금·멤버·사용량의 단위. 회사 또는 팀 하나. |
| 세션 | `session` | 통역이 시작되어 끝날 때까지의 1회 단위. 과금 대상. |
| 모드 | `mode: 'meeting' \| 'inperson'` | 모드 A = `meeting`, 모드 B = `inperson` |
| 세그먼트 | `segment` | 자막 한 덩어리. 원문+번역문+타임스탬프 한 쌍. |
| 소스/타깃 언어 | `sourceLang` / `targetLang` | **통역** 대상 언어. |
| UI 언어 | `uiLocale` | **화면** 표시 언어. 통역 언어와 **완전히 별개 개념** — 절대 섞지 말 것. |
| 크레딧 | `minutesBalance` | 남은 통역 가능 분(分). |
| 자막 뷰어 | `viewer` | 참가자가 링크로 들어와 자막만 보는 공개 화면. |

---

## 5. 개발 순서 (이 순서를 지킨다)

```
Phase 1  대면 모드 코어      마이크 → 번역 → 자막 화면. 로그인/과금 없음. 가치 검증.
Phase 2  계정 · 워크스페이스  Google 로그인, Firestore, 세션 목록
Phase 3  사용량 · 요금제      미터링, 하드 캡, 플랜, 결제
Phase 4  화상회의 모드        Recall.ai 어댑터 + 자막 링크 자동 게시
Phase 5  로그 · 고지 UI       스크립트 내보내기, 동의 배너, 보관 정책
Phase 6+ 확장                 헤드셋 음성 정교화, 다국어 동시 출력, Zoom 네이티브 자막
```

**Phase 1을 끝내기 전에 Phase 2 이후 코드를 쓰지 않는다.** 다만 Phase 1의 타입 정의는 처음부터 `docs/03`의 스키마를 따른다(나중에 갈아엎지 않기 위해).

---

## 6. 작업 완료 정의 (Definition of Done)

기능 하나가 "됐다"고 하려면 전부 만족해야 한다.

- [ ] 라이트/다크 모드 양쪽에서 정상 렌더 (`docs/05`)
- [ ] `ko` / `en` UI 문자열 양쪽 존재, 하드코딩 문자열 0개 (`docs/06`)
- [ ] 모바일 폭(375px)에서 레이아웃 붕괴 없음
- [ ] 키보드 포커스 링 보임, `prefers-reduced-motion` 존중
- [ ] 세션을 여는 기능이면 → 하드 캡·타임아웃·하트비트 포함 (`core.md §3-6`)
- [ ] 에러 상태에 "무엇이 잘못됐고 어떻게 고치는지" 문구 존재 (막연한 "오류가 발생했습니다" 금지)
- [ ] 빈 상태(empty state)가 다음 행동을 안내함
- [ ] `pnpm typecheck && pnpm lint` 통과

---

## 7. 이 문서의 신뢰도에 대하여

- **검증됨(2026-07-26 웹 조사):** `gpt-realtime-translate` $0.034/분, `gemini-3.5-live-translate` ≈$0.037/분, Recall.ai $0.50/시간. 출처는 `docs/07 §1`.
- **가설(검증 안 됨):** 요금제 가격대, 목표 마진, 예상 사용률, 시장 수요. `docs/07 §4`에 가설임을 명시했다. **실측 전까지 사실로 취급하지 말 것.**
- **미확정(결정 필요):** 결제 PG 선택, Realtime Worker 호스팅 확정, 브랜드명. `docs/02 §6` 참조.
